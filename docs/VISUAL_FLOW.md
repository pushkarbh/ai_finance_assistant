# Smart Tab Switching - Visual Flow

## User Experience Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    💬 CHAT TAB                              │
│                                                             │
│  User: "How is Apple stock doing?"                         │
│                                                             │
│  🤖 AI Assistant:                                          │
│  Apple (AAPL) is currently trading at $178.50, up 2.3%    │
│  today. The company has strong fundamentals with...        │
│                                                             │
│  📊 Agents used: market_analysis                           │
│                                                             │
│  ╔══════════════════════════════════════════════════════╗  │
│  ║ 🚀 Click the Market tab above to see detailed      ║  │
│  ║    AAPL information with charts and metrics!        ║  │
│  ╚══════════════════════════════════════════════════════╝  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                            ↓
                   User clicks Market tab
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    📈 MARKET TAB                            │
│                                                             │
│  ✨ Loaded AAPL from your question!                        │
│                                                             │
│  ┌────────────────────────────────────────────────────┐   │
│  │ Enter Ticker Symbol: [AAPL        ] [Look Up]     │   │
│  └────────────────────────────────────────────────────┘   │
│                                                             │
│  Apple Inc. (AAPL)                                         │
│  ┌──────────┬──────────┬─────────────┐                    │
│  │  Price   │ P/E Ratio│ Div. Yield  │                    │
│  │ $178.50  │   28.5   │   0.52%     │                    │
│  │  ▲ 2.3%  │          │             │                    │
│  └──────────┴──────────┴─────────────┘                    │
│                                                             │
│  Returns: 1D: +2.3%  1W: +5.2%  1M: +8.1%  6M: +15.3%    │
│                                                             │
│  [6-Month Price Chart visualization here]                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Behind the Scenes

```
USER QUERY: "How is Apple stock doing?"
     │
     ├─→ LangGraph Workflow
     │        │
     │        ├─→ Query Router
     │        │       │
     │        │       └─→ Routes to: market_analysis
     │        │
     │        └─→ Market Analysis Agent
     │                │
     │                ├─→ Fetches AAPL data
     │                └─→ Generates response
     │
     ├─→ handle_agent_tab_switching()
     │        │
     │        ├─→ Detects: primary_agent = "market_analysis"
     │        ├─→ Maps to: target_tab = 2 (Market)
     │        ├─→ extract_ticker_from_query("How is Apple...")
     │        │       └─→ Returns: "AAPL"
     │        │
     │        └─→ Sets session state:
     │                ├─→ switch_to_tab = 2
     │                ├─→ lookup_ticker = "AAPL"
     │                └─→ preload_data = {'ticker': 'AAPL'}
     │
     └─→ Display Response + Notification
              │
              └─→ User sees: "🚀 Click Market tab..."

USER CLICKS MARKET TAB (index=2)
     │
     ├─→ Tab renders with session_state.active_tab = 2
     │
     └─→ render_market_tab()
              │
              ├─→ Checks: preload_data.get('ticker')
              ├─→ Finds: "AAPL"
              ├─→ Sets: lookup_ticker = "AAPL"
              ├─→ Shows: "✨ Loaded AAPL from your question!"
              ├─→ Clears: preload_data = {}
              │
              └─→ Fetches and displays AAPL data
```

## Data Extraction Examples

### Ticker Extraction Flow
```
Input: "How is Apple doing?"
  ↓
query_upper = "HOW IS APPLE DOING?"
  ↓
Check dollar notation ($AAPL)? No
  ↓
Check common_tickers dictionary
  ↓
Found: "APPLE" → "AAPL"
  ↓
Output: "AAPL"
```

```
Input: "Tell me about $MSFT"
  ↓
query_upper = "TELL ME ABOUT $MSFT"
  ↓
Check dollar notation ($MSFT)? Yes!
  ↓
Regex: \$([A-Z]{1,5})\b matches "$MSFT"
  ↓
Output: "MSFT"
```

### Amount Extraction Flow
```
Input: "Save $50,000 for a house"
  ↓
Try pattern: \$([0-9,]+\.?[0-9]*)
  ↓
Match: "$50,000"
  ↓
Extract: "50,000"
  ↓
Parse: float("50000") = 50000.0
  ↓
Output: 50000.0
```

```
Input: "Need $1.5M for retirement"
  ↓
Try pattern: \$([0-9,]+\.?[0-9]*)\s*(?:million|M)
  ↓
Match: "$1.5M"
  ↓
Extract: "1.5"
  ↓
Parse: float("1.5") = 1.5
  ↓
Apply multiplier: 1.5 × 1,000,000
  ↓
Output: 1500000.0
```

## Tab Navigation States

```
┌─────────────────────────────────────────────────────────┐
│                  SESSION STATE                          │
├─────────────────────────────────────────────────────────┤
│  active_tab: 0          ← Currently on Chat             │
│  switch_to_tab: None    ← No pending switch             │
│  preload_data: {}       ← No data to preload            │
│  lookup_ticker: None    ← No ticker selected            │
└─────────────────────────────────────────────────────────┘

         User asks: "How is Tesla doing?"
                        ↓
┌─────────────────────────────────────────────────────────┐
│                  SESSION STATE                          │
├─────────────────────────────────────────────────────────┤
│  active_tab: 0          ← Still on Chat                 │
│  switch_to_tab: 2       ← Wants to go to Market         │
│  preload_data: {        ← Data prepared                 │
│    'ticker': 'TSLA'                                     │
│  }                                                       │
│  lookup_ticker: 'TSLA'  ← Ticker extracted              │
└─────────────────────────────────────────────────────────┘

         User clicks Market tab (or auto-reruns)
                        ↓
┌─────────────────────────────────────────────────────────┐
│                  SESSION STATE                          │
├─────────────────────────────────────────────────────────┤
│  active_tab: 2          ← Now on Market                 │
│  switch_to_tab: None    ← Switch completed              │
│  preload_data: {}       ← Data consumed & cleared       │
│  lookup_ticker: 'TSLA'  ← Ticker persists for lookup    │
└─────────────────────────────────────────────────────────┘
```

## Agent-to-Tab Mapping

```
┌──────────────────────┬─────────────┬──────────────────┐
│      AGENT TYPE      │  TAB INDEX  │    TAB NAME      │
├──────────────────────┼─────────────┼──────────────────┤
│ finance_qa           │      0      │ 💬 Chat         │
│                      │             │ (no switch)      │
├──────────────────────┼─────────────┼──────────────────┤
│ portfolio_analysis   │      1      │ 📊 Portfolio    │
├──────────────────────┼─────────────┼──────────────────┤
│ market_analysis      │      2      │ 📈 Market       │
│ news_synthesizer     │      2      │ 📈 Market       │
├──────────────────────┼─────────────┼──────────────────┤
│ goal_planning        │      3      │ 🎯 Goals        │
└──────────────────────┴─────────────┴──────────────────┘
```

## Complete Example Flow

```
╔═══════════════════════════════════════════════════════════╗
║  STEP 1: User in Chat Tab                                ║
╚═══════════════════════════════════════════════════════════╝
User types: "I want to save $100,000 for a house in 5 years"

        ↓ Process Query ↓

╔═══════════════════════════════════════════════════════════╗
║  STEP 2: Goal Planning Agent Responds                    ║
╚═══════════════════════════════════════════════════════════╝
Response: "To save $100,000 in 5 years, you'll need to
set aside about $1,667 per month, assuming a 6% annual
return. This is achievable with disciplined saving..."

Agents used: goal_planning

        ↓ Extract Data ↓

╔═══════════════════════════════════════════════════════════╗
║  STEP 3: Data Extraction                                 ║
╚═══════════════════════════════════════════════════════════╝
✓ Detected agent: goal_planning → Tab 3 (Goals)
✓ Extracted amount: $100,000
✓ Set preload_data = {'target_amount': 100000}

        ↓ Show Notification ↓

╔═══════════════════════════════════════════════════════════╗
║  STEP 4: User Sees Notification                          ║
╚═══════════════════════════════════════════════════════════╝
🚀 Click the Goals tab above to visualize your
   $100,000 goal with projections!

        ↓ User Clicks Tab ↓

╔═══════════════════════════════════════════════════════════╗
║  STEP 5: Goals Tab Loads with Pre-filled Data            ║
╚═══════════════════════════════════════════════════════════╝
✨ Pre-filled target amount $100,000 from your question!

Target Amount: [$100,000  ]  ← Pre-filled!
Current Savings: [$10,000   ]
Monthly Contribution: [$500     ]
Years Until Goal: [—————●——] 10 years
Risk Tolerance: [ Moderate (6%) ]

[Calculate Projection]  ← User can adjust and calculate

        ↓ User Adjusts & Calculates ↓

╔═══════════════════════════════════════════════════════════╗
║  STEP 6: See Projections                                 ║
╚═══════════════════════════════════════════════════════════╝
Projected Total: $152,435

🎉 You'll exceed your goal by $52,435!

[Growth chart showing trajectory over 10 years]
```

## Benefits Visualization

```
        BEFORE                          AFTER
┌────────────────────────┐    ┌────────────────────────┐
│ 1. Ask in Chat         │    │ 1. Ask in Chat         │
│ 2. Get answer          │    │ 2. Get answer          │
│ 3. Manually click tab  │    │ 3. See which tab       │
│ 4. Type ticker/amount  │    │ 4. Click suggested tab │
│ 5. Click lookup/submit │    │ 5. Data already loaded!│
│ 6. See results         │    │ ✓ 2 steps saved!       │
└────────────────────────┘    └────────────────────────┘
   5-6 user actions              3-4 user actions
```

## Summary Diagram

```
                    AI FINANCE ASSISTANT
              ┌─────────────────────────────┐
              │   Streamlit Web Interface   │
              └─────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
   ┌────▼────┐       ┌────▼────┐      ┌────▼────┐
   │ Chat    │       │Portfolio│      │ Market  │
   │         │       │         │      │         │
   │ Type Q  │       │ Charts  │      │ Charts  │
   │         │       │ Analysis│      │ Prices  │
   └─────────┘       └─────────┘      └─────────┘
        │
        │ "How is AAPL?"
        ▼
   ┌─────────────────────────────────────────┐
   │      LangGraph Workflow                 │
   │  ┌──────────┐         ┌──────────┐     │
   │  │  Router  │────────▶│  Market  │     │
   │  └──────────┘         │  Agent   │     │
   │                       └──────────┘     │
   └─────────────────────────────────────────┘
        │
        │ Response + metadata
        ▼
   ┌─────────────────────────────────────────┐
   │  Smart Tab Switching Logic              │
   │  • Detect agent type                    │
   │  • Extract ticker: "AAPL"               │
   │  • Map to tab: Market (2)               │
   │  • Show notification                    │
   └─────────────────────────────────────────┘
        │
        │ User clicks Market tab
        ▼
   ┌─────────────────────────────────────────┐
   │  Market Tab                             │
   │  • Detect preload_data                  │
   │  • Auto-fill: AAPL                      │
   │  • Fetch data                           │
   │  • Display charts                       │
   └─────────────────────────────────────────┘
```
